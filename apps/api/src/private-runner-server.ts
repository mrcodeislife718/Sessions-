import http from "node:http";
import { createDecipheriv, createHash, randomBytes, randomUUID } from "node:crypto";
import { Pool, type PoolClient } from "pg";

const port=Number(process.env.PRIVATE_RUNNER_PORT??4600),databaseUrl=process.env.DATABASE_URL??"postgresql://sessions:sessions@localhost:5432/sessions",maxBody=Number(process.env.SESSIONS_MAX_BODY_BYTES??1_048_576),leaseSeconds=Number(process.env.SESSIONS_PRIVATE_RUNNER_LEASE_SECONDS??120),maxLogBytes=Number(process.env.SESSIONS_ACTION_LOG_BYTES??2*1024*1024);
const pool=new Pool({connectionString:databaseUrl,max:Number(process.env.SESSIONS_PRIVATE_RUNNER_DB_POOL_MAX??10)});
class HttpError extends Error{constructor(public status:number,message:string){super(message)}}
type Identity={workspaceId:string;principalId:string;scopes:string[]};type Runner={id:string;workspace_id:string;labels:string[];max_concurrency:number;status:string};
function send(res:http.ServerResponse,status:number,body:unknown){res.writeHead(status,{"content-type":"application/json","cache-control":"no-store"});res.end(JSON.stringify(body))}
function hash(value:string){return createHash("sha256").update(value).digest("hex")}
async function body(req:http.IncomingMessage){const chunks:Buffer[]=[];let size=0;for await(const chunk of req){const b=Buffer.from(chunk);size+=b.length;if(size>maxBody)throw new HttpError(413,"request body too large");chunks.push(b)}if(!chunks.length)return{};try{return JSON.parse(Buffer.concat(chunks).toString("utf8"))}catch{throw new HttpError(400,"invalid JSON body")}}
async function apiIdentity(req:http.IncomingMessage):Promise<Identity>{const auth=req.headers.authorization;if(!auth?.startsWith("Bearer "))throw new HttpError(401,"bearer token required");const r=await pool.query(`select c.workspace_id,c.principal_id,c.scopes from api_credentials c join principals p on p.id=c.principal_id where c.token_hash=$1 and c.status='active' and p.status='active' and (c.expires_at is null or c.expires_at>now())`,[hash(auth.slice(7).trim())]);if(!r.rowCount)throw new HttpError(401,"invalid or expired credential");return{workspaceId:r.rows[0].workspace_id,principalId:r.rows[0].principal_id,scopes:r.rows[0].scopes??[]}}
function scope(i:Identity,s:string){if(!i.scopes.includes("*")&&!i.scopes.includes(s)&&!i.scopes.includes("sessions:write"))throw new HttpError(403,`missing scope: ${s}`)}
async function enterpriseAdmin(i:Identity){const r=await pool.query(`select m.role,p.kind,e.plan_key,e.status from workspace_memberships m join principals p on p.id=m.principal_id left join workspace_entitlements e on e.workspace_id=m.workspace_id where m.workspace_id=$1 and m.principal_id=$2`,[i.workspaceId,i.principalId]);const row=r.rows[0];if(!row||row.kind!=="human"||!["owner","admin"].includes(row.role))throw new HttpError(403,"private runner administration requires a human workspace owner or admin");if(row.status!=="active"||row.plan_key!=="enterprise")throw new HttpError(402,"private runners require an active Enterprise entitlement")}
async function runnerIdentity(req:http.IncomingMessage):Promise<Runner>{const auth=req.headers.authorization;if(!auth?.startsWith("Runner "))throw new HttpError(401,"runner token required");const r=await pool.query("update private_runners set last_seen_at=now(),updated_at=now() where token_hash=$1 and status='active' returning id,workspace_id,labels,max_concurrency,status",[hash(auth.slice(7).trim())]);if(!r.rowCount)throw new HttpError(401,"invalid or inactive runner token");return r.rows[0]}
function labels(v:unknown){if(!Array.isArray(v)||v.length>50)throw new HttpError(400,"labels must be an array of at most 50 values");const out=[...new Set(v.map(x=>String(x).trim()).filter(Boolean))];if(out.some(x=>!/^[A-Za-z0-9_.:-]{1,64}$/.test(x)))throw new HttpError(400,"runner labels contain an invalid value");return out}
function actionKey(){const raw=process.env.SESSIONS_ACTION_SECRET_KEY;if(!raw)throw new HttpError(503,"Actions secret encryption key is not configured");const key=/^[a-f0-9]{64}$/i.test(raw)?Buffer.from(raw,"hex"):Buffer.from(raw,"base64");if(key.length!==32)throw new HttpError(503,"Actions secret encryption key must decode to 32 bytes");return key}
function decrypt(row:any){const d=createDecipheriv("aes-256-gcm",actionKey(),Buffer.from(row.nonce,"base64"));d.setAuthTag(Buffer.from(row.auth_tag,"base64"));return Buffer.concat([d.update(Buffer.from(row.ciphertext,"base64")),d.final()]).toString("utf8")}
async function audit(workspaceId:string,principalId:string|null,action:string,resourceId:string,metadata:any={}){await pool.query("insert into audit_events(id,workspace_id,principal_id,action,resource_type,resource_id,outcome,metadata) values($1,$2,$3,$4,'private_runner',$5,'allowed',$6)",[`audit_${randomUUID()}`,workspaceId,principalId,action,resourceId,JSON.stringify(metadata)])}
async function assigned(r:Runner,runId:string){const q=await pool.query("select * from action_runs where id=$1 and workspace_id=$2 and assigned_private_runner_id=$3 and status='running' and runner_lease_expires_at>now()",[runId,r.workspace_id,r.id]);if(!q.rowCount)throw new HttpError(409,"job is not actively leased to this runner");return q.rows[0]}
async function claim(r:Runner){const client=await pool.connect();try{await client.query("begin");await client.query("update action_runs set status='queued',assigned_private_runner_id=null,runner_lease_expires_at=null,started_at=null where workspace_id=$1 and execution_target in ('private','either') and status='running' and assigned_private_runner_id is not null and runner_lease_expires_at<=now()",[r.workspace_id]);const active=Number((await client.query("select count(*)::int n from action_runs where assigned_private_runner_id=$1 and status='running' and runner_lease_expires_at>now()",[r.id])).rows[0].n);if(active>=r.max_concurrency){await client.query("rollback");return null}const q=await client.query(`select id,repository_id,commit_id,workspace_id,workflow_id,workflow_name,runner_labels from action_runs where workspace_id=$1 and execution_target in ('private','either') and status='queued' and $2::text[] @> runner_labels order by created_at for update skip locked limit 1`,[r.workspace_id,r.labels??[]]);if(!q.rowCount){await client.query("rollback");return null}const run=q.rows[0];await client.query("update action_runs set status='running',assigned_private_runner_id=$2,runner_lease_expires_at=now()+($3::text||' seconds')::interval,started_at=coalesce(started_at,now()) where id=$1",[run.id,r.id,leaseSeconds]);const checks=await client.query("select id,name,category,container_image,command_argv,timeout_seconds,network_policy,secret_names from action_checks where action_run_id=$1 order by id",[run.id]);await client.query("commit");return{...run,leaseSeconds,checks:checks.rows}}catch(e){await client.query("rollback").catch(()=>undefined);throw e}finally{client.release()}}
async function manifest(r:Runner,runId:string){const run=await assigned(r,runId);const cp=(await pool.query("select record from sessions_repository_checkpoints where repository_id=$1 and checkpoint_id=$2",[run.repository_id,run.commit_id])).rows[0]?.record;if(!cp?.sourceManifestId)throw new HttpError(409,"job checkpoint has no source manifest");const m=(await pool.query("select manifest from repository_manifests where repository_id=$1 and id=$2",[run.repository_id,cp.sourceManifestId])).rows[0]?.manifest;if(!m)throw new HttpError(409,"job source manifest is missing");return{repositoryId:run.repository_id,commitId:run.commit_id,sourceDigest:cp.sourceDigest,manifest:m}}
async function complete(r:Runner,runId:string,b:any){await assigned(r,runId);const results=Array.isArray(b.checks)?b.checks:[];const expected=(await pool.query("select id from action_checks where action_run_id=$1",[runId])).rows.map(x=>String(x.id));if(results.length!==expected.length||results.some((x:any)=>!expected.includes(String(x.id))))throw new HttpError(400,"completion must provide exactly one result for every job check");const client=await pool.connect();try{await client.query("begin");let failed=false;for(const x of results){const conclusion=String(x.conclusion)==="success"?"success":"failure";failed||=conclusion!=="success";const log=String(x.log??"");if(Buffer.byteLength(log)>maxLogBytes)throw new HttpError(413,"private runner log exceeds configured limit");await client.query("update action_checks set status='completed',conclusion=$2,summary=$3,evidence=$4,log_text=$5,exit_code=$6,started_at=coalesce(started_at,now()),completed_at=now() where id=$1 and action_run_id=$7",[x.id,conclusion,String(x.summary??"Private runner execution").slice(0,1000),JSON.stringify({...x.evidence,privateRunnerId:r.id}),log,Number.isInteger(x.exitCode)?x.exitCode:null,runId])}await client.query("update action_runs set status='completed',conclusion=$2,completed_at=now(),runner_lease_expires_at=null where id=$1 and assigned_private_runner_id=$3",[runId,failed?"failure":"success",r.id]);await client.query("commit");await audit(r.workspace_id,null,"private_runner.job_complete",r.id,{runId,conclusion:failed?"failure":"success"});return{runId,status:"completed",conclusion:failed?"failure":"success"}}catch(e){await client.query("rollback").catch(()=>undefined);throw e}finally{client.release()}}

const server=http.createServer(async(req,res)=>{try{const url=new URL(req.url??"/",`http://${req.headers.host??"localhost"}`);if(req.method==="GET"&&url.pathname==="/health"){await pool.query("select 1");return send(res,200,{ok:true,service:"sessions-private-runners"})}
if(url.pathname.startsWith("/api/private-runner/")){const r=await runnerIdentity(req);if(req.method==="POST"&&url.pathname==="/api/private-runner/claim")return send(res,200,{job:await claim(r)});let m=url.pathname.match(/^\/api\/private-runner\/jobs\/([^/]+)\/heartbeat$/);if(m&&req.method==="POST"){await assigned(r,m[1]);await pool.query("update action_runs set runner_lease_expires_at=now()+($2::text||' seconds')::interval where id=$1 and assigned_private_runner_id=$3",[m[1],leaseSeconds,r.id]);return send(res,200,{runId:m[1],leaseSeconds})}m=url.pathname.match(/^\/api\/private-runner\/jobs\/([^/]+)\/manifest$/);if(m&&req.method==="GET")return send(res,200,await manifest(r,m[1]));m=url.pathname.match(/^\/api\/private-runner\/jobs\/([^/]+)\/objects\/([^/]+)$/);if(m&&req.method==="GET"){const run=await assigned(r,m[1]);const o=(await pool.query("select object_id,digest,content from sessions_repository_objects where repository_id=$1 and object_id=$2",[run.repository_id,decodeURIComponent(m[2])])).rows[0];if(!o)throw new HttpError(404,"source object not found");const content=Buffer.from(o.content);if(hash(content.toString("binary"))!==o.digest&&createHash("sha256").update(content).digest("hex")!==o.digest)throw new HttpError(500,"stored source object failed digest verification");return send(res,200,{objectId:o.object_id,digest:o.digest,size:content.length,contentBase64:content.toString("base64")})}m=url.pathname.match(/^\/api\/private-runner\/jobs\/([^/]+)\/secrets$/);if(m&&req.method==="POST"){await assigned(r,m[1]);const requested=labels((await body(req)).names);const allowed=new Set<string>();for(const row of (await pool.query("select secret_names from action_checks where action_run_id=$1",[m[1])).rows)for(const name of row.secret_names??[])allowed.add(String(name));if(requested.some(x=>!allowed.has(x)))throw new HttpError(403,"job requested a secret not declared by its workflow");const rows=await pool.query("select name,ciphertext,nonce,auth_tag from repository_action_secrets where workspace_id=$1 and name=any($2::text[]) and repository_id=(select repository_id from action_runs where id=$3)",[r.workspace_id,requested,m[1]]);if(rows.rowCount!==requested.length)throw new HttpError(409,"one or more required workflow secrets are missing");return send(res,200,{secrets:Object.fromEntries(rows.rows.map(x=>[x.name,decrypt(x)]))})}m=url.pathname.match(/^\/api\/private-runner\/jobs\/([^/]+)\/complete$/);if(m&&req.method==="POST")return send(res,200,await complete(r,m[1],await body(req)));throw new HttpError(404,"not found")}
const i=await apiIdentity(req);scope(i,req.method==="GET"?"sessions:read":"sessions:write");await enterpriseAdmin(i);if(url.pathname==="/api/private-runners"){if(req.method==="GET"){const q=await pool.query("select id,name,labels,max_concurrency,status,last_seen_at,created_at,updated_at from private_runners where workspace_id=$1 order by name",[i.workspaceId]);return send(res,200,q.rows)}if(req.method==="POST"){const b=await body(req),name=String(b.name??"").trim();if(!/^[A-Za-z0-9_. -]{1,120}$/.test(name))throw new HttpError(400,"valid runner name is required");const runnerLabels=labels(b.labels??[]),maxConcurrency=Number(b.maxConcurrency??1);if(!Number.isInteger(maxConcurrency)||maxConcurrency<1||maxConcurrency>64)throw new HttpError(400,"maxConcurrency must be 1-64");const token=`runner_${randomBytes(32).toString("hex")}`;const q=await pool.query("insert into private_runners(workspace_id,name,token_hash,labels,max_concurrency,created_by) values($1,$2,$3,$4,$5,$6) returning id,name,labels,max_concurrency,status,created_at",[i.workspaceId,name,hash(token),runnerLabels,maxConcurrency,i.principalId]);await audit(i.workspaceId,i.principalId,"private_runner.register",q.rows[0].id,{name,labels:runnerLabels,maxConcurrency});return send(res,201,{...q.rows[0],token})}}
let m=url.pathname.match(/^\/api\/private-runners\/([^/]+)\/(disable|revoke|enable)$/);if(m&&req.method==="POST"){const status=m[2]==="revoke"?"revoked":m[2]==="disable"?"disabled":"active";const q=await pool.query("update private_runners set status=$3,updated_at=now() where id=$1 and workspace_id=$2 and status<>'revoked' returning id,name,status",[m[1],i.workspaceId,status]);if(!q.rowCount)throw new HttpError(404,"runner not found or permanently revoked");await audit(i.workspaceId,i.principalId,`private_runner.${m[2]}`,m[1]);return send(res,200,q.rows[0])}
m=url.pathname.match(/^\/api\/repositories\/([^/]+)\/runner-policy$/);if(m){const repo=decodeURIComponent(m[1]);const exists=await pool.query("select lifecycle_status from hosted_repositories where id=$1 and workspace_id=$2",[repo,i.workspaceId]);if(!exists.rowCount)throw new HttpError(404,"repository not found");if(req.method==="GET"){const q=await pool.query("select * from repository_runner_policies where repository_id=$1 and workspace_id=$2",[repo,i.workspaceId]);return send(res,200,q.rows[0]??{repositoryId:repo,executionTarget:"hosted",requiredLabels:[]})}if(req.method==="PUT"){if(exists.rows[0].lifecycle_status!=="active")throw new HttpError(409,"repository is read-only");const b=await body(req),target=String(b.executionTarget??"hosted");if(!["hosted","private","either"].includes(target))throw new HttpError(400,"executionTarget must be hosted, private, or either");const required=labels(b.requiredLabels??[]);const q=await pool.query(`insert into repository_runner_policies(workspace_id,repository_id,execution_target,required_labels,created_by) values($1,$2,$3,$4,$5) on conflict(repository_id) do update set execution_target=excluded.execution_target,required_labels=excluded.required_labels,updated_at=now() returning *`,[i.workspaceId,repo,target,required,i.principalId]);await audit(i.workspaceId,i.principalId,"private_runner.policy_update",repo,{executionTarget:target,requiredLabels:required});return send(res,200,q.rows[0])}}
throw new HttpError(404,"not found")}catch(e){const status=e instanceof HttpError?e.status:500;const message=e instanceof Error?e.message:"internal error";console.error(JSON.stringify({level:"error",service:"sessions-private-runners",status,message}));return send(res,status,{error:status>=500?"internal error":message})}});
server.listen(port,"0.0.0.0",()=>console.log(JSON.stringify({level:"info",event:"private-runners.started",port,leaseSeconds})));async function shutdown(){server.close(async()=>{await pool.end();process.exit(0)});setTimeout(()=>process.exit(1),10_000).unref()}process.on("SIGTERM",()=>void shutdown());process.on("SIGINT",()=>void shutdown());
