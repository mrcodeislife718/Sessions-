export type RepositoryCommand="status"|"history"|"integrity"|"tags"|"remotes"|"fetch"|"pull"|"push"|"revert";
export class RepositoryClient{constructor(private readonly baseUrl=""){}async command<T=unknown>(repositoryId:string,command:RepositoryCommand,input:Record<string,unknown>={}){const response=await fetch(`${this.baseUrl}/api/repositories/${encodeURIComponent(repositoryId)}/source-control/${command}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input)});const text=await response.text();if(!response.ok)throw new Error(text||`Repository command failed: ${response.status}`);return(text?JSON.parse(text):{}) as T}}
