{
  "name": "sessions",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native source control and execution infrastructure for AI systems, AI agents, and humans.",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "tsc -b packages/shared packages/codevault-core packages/timeline-engine packages/verification-engine",
    "typecheck": "tsc -b packages/shared packages/codevault-core packages/timeline-engine packages/verification-engine --pretty false",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
