{
  "name": "sessions-vscode",
  "displayName": "Sessions",
  "description": "Git-familiar source control and collaboration with persistent context, AI provenance, verification, execution visibility, and recovery.",
  "version": "0.1.0",
  "private": true,
  "engines": { "vscode": "^1.95.0" },
  "main": "./dist/extension.js",
  "activationEvents": ["onStartupFinished"],
  "contributes": {
    "configuration": {
      "title": "Sessions",
      "properties": {
        "sessions.apiUrl": {
          "type": "string",
          "default": "http://localhost:4000",
          "description": "Sessions API URL. Hosted tokens are stored separately in VS Code SecretStorage."
        }
      }
    },
    "commands": [
      { "command": "sessions.refresh", "title": "Sessions: Refresh" },
      { "command": "sessions.connectHosted", "title": "Sessions: Sign In" },
      { "command": "sessions.disconnectHosted", "title": "Sessions: Sign Out" },
      { "command": "sessions.billing", "title": "Sessions: Billing and Plans" },
      { "command": "sessions.start", "title": "Sessions: Start Session" },
      { "command": "sessions.stageAll", "title": "Sessions: Stage All Changes" },
      { "command": "sessions.unstageAll", "title": "Sessions: Unstage All Changes" },
      { "command": "sessions.checkpoint", "title": "Sessions: Commit Changes" },
      { "command": "sessions.switchWorkstream", "title": "Sessions: Switch Branch" },
      { "command": "sessions.verify", "title": "Sessions: Run Checks" },
      { "command": "sessions.integrity", "title": "Sessions: Verify Repository Integrity" },
      { "command": "sessions.timeline", "title": "Sessions: View History" },
      { "command": "sessions.rollback", "title": "Sessions: Restore Commit" }
    ],
    "viewsContainers": {
      "activitybar": [{ "id": "sessions", "title": "Sessions", "icon": "resources/icon.svg" }]
    },
    "views": {
      "sessions": [{ "id": "sessions.overview", "name": "Source Control" }]
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@sessions/native-repository": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.95.0",
    "typescript": "^5.9.0"
  }
}
