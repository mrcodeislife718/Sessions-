FROM node:22-alpine
WORKDIR /app
RUN apk add --no-cache docker-cli
RUN corepack enable
COPY . .
RUN pnpm install
RUN pnpm --filter @sessions/runner build
ENV NODE_ENV=production
CMD ["pnpm", "--filter", "@sessions/runner", "start:executor"]
